{
  "name": "app-live-debug",
  "displayName": "App Live Debug",
  "description": "Bridge between Helios app and Cursor for live debugging: receive prompts via local API, copy to clipboard, and work with .cursor/tickets.json",
  "version": "0.1.0",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "appLiveDebug.copyPendingPrompt",
        "title": "App Live Debug : Copier le prompt en attente"
      },
      {
        "command": "appLiveDebug.pickTicketToComposer",
        "title": "App Live Debug : Choisir un ticket pour Composer"
      }
    ],
    "configuration": {
      "title": "App Live Debug",
      "properties": {
        "appLiveDebug.serverPort": {
          "type": "number",
          "default": 8765,
          "description": "Port for the local HTTP server that receives prompts from Helios."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.3.0"
  }
}
